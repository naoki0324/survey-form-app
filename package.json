{
  "name": "survey-form-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "dev:op": "op run --env-file=.env.op -- ./node_modules/.bin/next dev",
    "build": "next build",
    "build:op": "op run --env-file=.env.op -- ./node_modules/.bin/next build",
    "start": "next start",
    "start:op": "op run --env-file=.env.op -- ./node_modules/.bin/next start"
  },
  "dependencies": {
    "next": "^14.2.0",
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "resend": "^3.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "typescript": "^5.0.0"
  }
}
